import 'package:flutter/material.dart';

class NavigationModel {
  IconData icon;
  String label;
  String slug;

  NavigationModel({
    required this.icon,
    required this.label,
    required this.slug,
  });
}
